pipeline {
    agent none

    stages {
        stage('Check exist container') {
            steps {
                def foo = "Check exist container"
                sh "echo ${foo}"
            }
        }
    }
}
